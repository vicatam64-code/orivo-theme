<section class="section"><div class="wrap" style="max-width:820px;">
  <div class="section-head reveal" style="margin-bottom:24px;"><h1 style="font-family:var(--font-display);font-weight:600;font-size:clamp(34px,5vw,54px);">{{ page.title }}</h1></div>
  <div class="rte reveal">{{ page.content }}</div>
</div></section>
{% schema %}{ "name": "Page" }{% endschema %}
